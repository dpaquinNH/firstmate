# New test test_pause_covers_own_background_jobs_in_every_scaffold run against BASE generator (9ba29db) -> expected to fail
not ok - ship brief does not extend the pause verb to the worker's own background jobs

# Same test against TARGET generator (6cb959a):
ok - fm-brief.sh: every scaffold lets a declared, bounded pause cover the worker's own background jobs
